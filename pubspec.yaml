name: just_crypto
description: Dart cryptography package backed by Rust native assets, with domain-specific APIs, typed key material, isolates, and cross-platform validation.
version: 1.0.0
# repository: https://github.com/my_org/my_repo
topics:
  - cryptography
  - ffi
  - rust

environment:
  sdk: ^3.11.3

# Add regular dependencies here.
dependencies:
  code_assets: ^1.0.0
  ffi: ^2.2.0
  hooks: ^1.0.2
  logging: ^1.3.0
  native_toolchain_rust: ^1.0.3
  # path: ^1.9.0

dev_dependencies:
  cryptography: ^2.7.0
  ffigen: ^20.1.1
  lints: ^6.0.0
  test: ^1.25.6
